<div class="bg-mountain-silhouette" aria-hidden="true"></div>

<style>
  .bg-mountain-silhouette {
    position: fixed;
    inset: 0;
    z-index: -1;
    background-color: #fafaf9;
    /* 背景に薄いグリッドを敷きつつ */
    background-image: radial-gradient(rgba(0, 0, 0, 0.02) 1px, transparent 1px);
    background-size: 50px 50px;
  }

  .bg-mountain-silhouette::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 20vh;
    background: #2d5a27; /* 森の緑 */
    opacity: 0.03; /* 3%まで落として「言われれば山」くらいの薄さに */
    /* 山の形に切り抜き（左から右へ連なる山脈） */
    clip-path: polygon(0% 100%, 0% 80%, 15% 60%, 30% 85%, 50% 40%, 70% 75%, 85% 55%, 100% 85%, 100% 100%);
    filter: blur(2px); /* 境界を少しぼかして目に優しく */
  }
</style>